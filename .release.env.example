# Copy this to .release.env for local release builds. .release.env is ignored.

APP_VERSION=0.1.0
BUILD_NUMBER=1
CODESIGN_IDENTITY="Developer ID Application: Your Name (TEAMID)"
NOTARY_PROFILE=notarytool
